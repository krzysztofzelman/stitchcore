with:
          host: 31.3.218.196
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 2022
          script: |
            cd /var/www/stitchcore
            git pull
            docker compose up -d --build